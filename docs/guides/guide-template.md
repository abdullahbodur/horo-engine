# <Workflow> Guide

<!-- Copy this file to docs/guides/<topic>.md and replace every placeholder.
Keep examples generic: use <organization-key>, <project-key>, and
/absolute/path/to/project rather than personal accounts or local paths.
Link normative contracts instead of duplicating them. Remove these comments.
-->

## Purpose

Describe the outcome, audience, and scope in one short paragraph.

## Prerequisites

List required tools, permissions, configuration, and supported environments.
Distinguish required components from optional integrations.

## Workflow

### 1. Prepare

Explain the starting state and inputs. Define placeholders before using them.

### 2. Configure and run

Provide complete, minimal examples. Identify the file or UI location to edit,
the directory from which commands run, and any persistent side effects.
Keep credentials out of examples and version control.

### 3. Verify

Give the expected result and the evidence that establishes success.
Separate partial success from complete validation.

## Troubleshooting

| Symptom | Check | Recovery |
| --- | --- | --- |
| <Observable failure> | <Evidence to inspect> | <Bounded next step> |

## Limitations

State unsupported behavior, environment-specific differences, and work that
requires a separate workflow. Do not present an unverified workaround as a fix.

## Validation Record

Record the verification date, relevant versions/platforms, checks actually run,
and remaining gaps. Do not include personal identifiers or raw private logs.

## References

Link the owning architecture contracts, repository instructions, and primary
external documentation used by this guide.
