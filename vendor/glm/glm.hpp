// GLM placeholder — download the real GLM (https://github.com/g-truc/glm) into
// this directory. GLM is header-only. The full source is not committed to keep
// the repo small. For correctness-reference use only; Monolith ships its own
// math library.
#pragma once
#pragma message(                                                               \
    "GLM stub: replace with real GLM headers from https://github.com/g-truc/glm")
