[pytest]
testpaths = tests/python
markers =
    script: fast unit tests for Python CI/test infrastructure
    ci_smoke: pytest-native smoke tests that require compiled C++ artifacts or CMake build directories
